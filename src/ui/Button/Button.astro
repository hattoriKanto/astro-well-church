---
import "./Button.css";

type Props = {
  href: string;
  backgroundColor?: string;
};

const { href, backgroundColor = "var(--white)" } = Astro.props;
---

<a class="button" href={href} style={{ backgroundColor }}>
  <span class="top-key" style={{ backgroundColor }}></span>
  <span class="text">
    <slot />
  </span>
  <span class="bottom-key-1" style={{ backgroundColor }}></span>
  <span class="bottom-key-2" style={{ backgroundColor }}></span>
</a>
